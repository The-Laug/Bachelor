import "complexinner_summationDomain.vpr"
method try(x:Int, y: Int) 
{
    var i: Int
    var N: Int
    var j: Int

    assume 0 < i < N;

    // Complex inner function
    assert 2 * simplesum(0, 5) + 3 * powerSum2(0,5) == 195
    assert powerSum2(1,3) + 2 * simplesum(1, 3) + (1*(3-1+1)) == 29

}
