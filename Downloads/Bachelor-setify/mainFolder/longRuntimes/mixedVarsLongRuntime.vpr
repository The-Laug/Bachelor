method try() 
{
    var i: Int
    var N: Int

    assume 0 < i < N;

    // Summation with mixed variables
    assert for j=1 to 3 sum (j*i + 2*j*N) == 12*N +6*i
    // assert for j=0 to i sum (j*N) == N*i*(i+1)/2


}
