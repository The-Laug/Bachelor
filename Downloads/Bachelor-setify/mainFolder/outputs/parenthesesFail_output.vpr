import "parenthesesFail_summationDomain.vpr"
method try() 
{
    var i: Int
    var N: Int

    assume 0 < i < N;

       // Complex expressions
    assert genericSum0(0, 3) + (1*(3-0+1))*(j + 2)) == 74
    assert genericSum1(1, 5) - simplesum(1, 5) + (1*(5-1+1))*(j + 3)) == 230
}
