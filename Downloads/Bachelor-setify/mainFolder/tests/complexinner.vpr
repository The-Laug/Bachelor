method try(x:Int, y: Int) 
{
    var i: Int
    var N: Int
    var j: Int

    assume 0 < i < N;

    // Complex inner function
    assert for j=0 to 5 sum (2*j + 3*j^2) == 195
    assert for j=1 to 3 sum (j^2 + 2*j + 1) == 29

}
