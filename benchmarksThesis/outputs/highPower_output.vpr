import "highPower_summationDomain.vpr"
method try(x:Int, y: Int) 
{
    var i: Int
    var N: Int
    var j: Int

    assume 0 < i < N;

    // Polynomial summation
    assert powerSum2(0,10) == 385
    assert powerSum3(1,5) == 225

    // Trigonometric (if supported)
    // assert genericSum0(0, 2)) == sin(0) + sin(1) + sin(2)
}
