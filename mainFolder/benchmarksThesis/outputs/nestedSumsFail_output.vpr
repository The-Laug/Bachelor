import "nestedSumsFail_summationDomain.vpr"
method try() 
{
    var i: Int
    var N: Int

    assume 0 < i < N;

    // Summation with nested sums
    assert genericSum0(0, 2) + (k*(2-0+1))) == 18
    assert j  * (k * genericSum1(1, 3) - (1*(3-1+1)) + (1*(3-1+1)))) == 90

}
