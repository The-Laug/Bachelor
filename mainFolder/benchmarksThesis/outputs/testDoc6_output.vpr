import "testDoc6_summationDomain.vpr"

method try() 
{
    var i: Int
    var N: Int
    var z: Int
    var x: Int
    var y: Int


    assume 0 < i < N;

    // Summation with constants
    assert 4 * z  * 3 * (x*(0-4+1)) + (1*(4-0+1)) - genericSum0(0, 4) + simplesum(0, 4) ==
    assert genericSum1(0, 4) ==2

}
