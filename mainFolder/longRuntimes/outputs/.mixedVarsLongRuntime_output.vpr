import "mixedVarsLongRuntime_summationDomain.vpr"
method try() 
{
    var i: Int
    var N: Int

    assume 0 < i < N;

    // Summation with mixed variables
    assert i * simplesum(1, 3) + 2 * N * simplesum(1, 3) == 12*N +6*i
    assert N * simplesum(0, i) == N*i*(i+1)/2


}
